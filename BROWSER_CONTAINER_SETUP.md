# browser_container を利用可能にする方法

この実行環境では、エージェント側から `browser_container` ツールを**後付けで有効化することはできません**。  
利用可否は、セッション作成時のツール構成で決まります。

## 1) まず確認すること

- セッションで `browser_container` ツールが配布されているか
- 実行環境にブラウザ実行用のサンドボックス設定があるか
- スクリーンショット保存先（artifact path）が有効か

## 2) 有効化の基本方針（運用側で設定）

1. エージェント起動時に `browser_container` をツール一覧へ含める
2. 必要なブラウザ依存（Chromium 等）を事前にプロビジョニングする
3. ネットワーク/ローカルURLアクセスのポリシーを許可する
4. 画像出力先ディレクトリをセッションにマウントする

## 3) 代替手段（browser_container が無い場合）

- CI で Playwright/Puppeteer を使ってスクリーンショットを生成する
- ローカルPCで `index.html` を開いて手動撮影する
- デザイン差分だけなら HTML/CSS の変更点レビューで代替する

## 4) このリポジトリでの実務的な対応

- フロントエンド変更時は、`browser_container` が使える実行プロファイルで再実行する
- 使えないプロファイルの場合は、PR に「ツール未提供のため撮影不可」と明記する

